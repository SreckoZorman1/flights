const aircrafts_dataFields = {
  id: { type: 'id', label: 'ID' },

  model: { type: 'string', label: 'model' },

  range: { type: 'int', label: 'range' },

  constraint: { type: 'string', label: 'constraint' },

  aircraft_code: { type: 'string', label: 'aircraft_code' },
};

export default aircrafts_dataFields;
