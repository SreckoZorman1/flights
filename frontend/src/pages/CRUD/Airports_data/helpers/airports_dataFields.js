const airports_dataFields = {
  id: { type: 'id', label: 'ID' },

  airport_name: { type: 'string', label: 'airport_name' },

  city: { type: 'string', label: 'city' },

  timezone: { type: 'string', label: 'timezone' },

  airport_code: { type: 'string', label: 'airport_code' },
};

export default airports_dataFields;
